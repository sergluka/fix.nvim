# Synthetic FIX samples

These samples are independently constructed from public FIX specifications and
the bundled public dictionaries. Never copy, anonymize, or transform source
logs into this directory.

`big.fix` is a generated synthetic FIX 4.4 corpus with synthetic identifiers,
timestamps, message selection, and field values. It was created specifically
for this project and is not derived from a source log.
